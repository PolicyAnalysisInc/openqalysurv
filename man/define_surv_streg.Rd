% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_param_streg.r
\name{define_surv_streg}
\alias{define_surv_streg}
\title{Define Parametric Distribution from Stata streg Parameters}
\usage{
define_surv_streg(distribution, ..., metric = c("aft", "ph"))
}
\arguments{
\item{distribution}{one of \code{"exponential"}, \code{"weibull"},
\code{"lognormal"}, \code{"loglogistic"}, \code{"ggamma"} (AFT metric), or
\code{"gompertz"} (PH metric only).}

\item{...}{distribution parameters from streg output.
The \verb{_cons} coefficient should be passed as \code{cons}.
See details for parameter names by distribution.}

\item{metric}{either \code{"aft"} or \code{"ph"}, indicating which
metric was used to fit the model.}
}
\value{
a \code{surv_parametric} object.
}
\description{
Define a parametric survival distribution using parameter estimates
from Stata's \code{streg} output. Parameters are converted from the
AFT or PH parameterization to flexsurv's native parameterization.
}
\details{
\subsection{AFT metric parameters}{\tabular{lll}{
   \strong{Distribution} \tab \strong{Parameters} \tab \strong{Conversion} \cr
   exponential \tab \code{cons} \tab \code{rate = exp(-cons)} \cr
   weibull \tab \code{cons}, \code{ln_p} \tab \code{shape = exp(ln_p)}, \code{scale = exp(cons)} \cr
   lognormal \tab \code{cons}, \code{sigma} \tab \code{meanlog = cons}, \code{sdlog = sigma} \cr
   loglogistic \tab \code{cons}, \code{gamma} \tab \code{shape = gamma}, \code{scale = exp(cons)} \cr
   ggamma \tab \code{cons}, \code{sigma}, \code{kappa} \tab \code{mu = cons}, \code{sigma = sigma}, \code{Q = kappa} \cr
}

}

\subsection{PH metric parameters}{\tabular{lll}{
   \strong{Distribution} \tab \strong{Parameters} \tab \strong{Conversion} \cr
   exponential \tab \code{cons} \tab \code{rate = exp(cons)} \cr
   weibull \tab \code{cons}, \code{ln_p} \tab \code{shape = exp(ln_p)}, \code{scale = exp(cons)} \cr
   gompertz \tab \code{cons}, \code{gamma} \tab \code{shape = gamma}, \code{rate = exp(cons)} \cr
}

}

\subsection{Survival function formulations}{
\subsection{AFT metric}{
\itemize{
\item \strong{Exponential}: \code{S(t) = exp(-exp(-cons) * t)}
\item \strong{Weibull}: \code{S(t) = exp(-(t / exp(cons))^exp(ln_p))}
\item \strong{Lognormal}: \code{S(t) = 1 - Phi((log(t) - cons) / sigma)} where
\code{Phi()} is the standard normal CDF
\item \strong{Log-logistic}: \code{S(t) = 1 / (1 + (t / exp(cons))^gamma)}
\item \strong{Generalized gamma}: No simple closed form;
\code{mu = cons}, \code{sigma = sigma}, \code{Q = kappa}
}
}

\subsection{PH metric}{
\itemize{
\item \strong{Exponential}: \code{S(t) = exp(-exp(cons) * t)}
\item \strong{Weibull}: \code{S(t) = exp(-exp(cons) * t^exp(ln_p))}
\item \strong{Gompertz}: hazard \code{h(t) = exp(cons) * exp(gamma * t)},
survival \code{S(t) = exp(-(exp(cons) / gamma) * (exp(gamma * t) - 1))}
}
}

}
}
\examples{

define_surv_streg("exponential", cons = 3, metric = "aft")
define_surv_streg("weibull", cons = 3, ln_p = 0.405, metric = "aft")
define_surv_streg("gompertz", cons = -3, gamma = 0.05, metric = "ph")

}
