% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_param_survreg.r
\name{define_surv_survreg}
\alias{define_surv_survreg}
\title{Define Parametric Distribution from survreg Parameters}
\usage{
define_surv_survreg(distribution, intercept, scale = 1)
}
\arguments{
\item{distribution}{one of \code{"exponential"}, \code{"weibull"},
\code{"lognormal"}, or \code{"loglogistic"}.}

\item{intercept}{the intercept from the survreg model
(\code{coef(model)["(Intercept)"]}).}

\item{scale}{the scale parameter from the survreg model
(\code{model$scale}). Defaults to 1 (used for exponential).}
}
\value{
a \code{surv_parametric} object.
}
\description{
Define a parametric survival distribution using parameter estimates
from R's \code{\link[survival:survreg]{survival::survreg()}} output. Parameters are converted
from the AFT parameterization to flexsurv's native parameterization.
}
\details{
The following conversions are applied:\tabular{lll}{
   \strong{survreg distribution} \tab \strong{flexsurv distribution} \tab \strong{Conversion} \cr
   exponential \tab exp \tab \code{rate = exp(-intercept)} \cr
   weibull \tab weibull \tab \code{shape = 1/scale}, \code{scale = exp(intercept)} \cr
   lognormal \tab lnorm \tab \code{meanlog = intercept}, \code{sdlog = scale} \cr
   loglogistic \tab llogis \tab \code{shape = 1/scale}, \code{scale = exp(intercept)} \cr
}

\subsection{Survival function formulations}{

In terms of survreg parameters:
\itemize{
\item \strong{Exponential}: \code{S(t) = exp(-exp(-intercept) * t)}
\item \strong{Weibull}: \code{S(t) = exp(-(t / exp(intercept))^(1/scale))}
\item \strong{Lognormal}: \code{S(t) = 1 - Phi((log(t) - intercept) / scale)} where
\code{Phi()} is the standard normal CDF
\item \strong{Log-logistic}: \code{S(t) = 1 / (1 + (t / exp(intercept))^(1/scale))}
}
}
}
\examples{

define_surv_survreg("exponential", intercept = 3)
define_surv_survreg("weibull", intercept = 3, scale = 0.8)

}
