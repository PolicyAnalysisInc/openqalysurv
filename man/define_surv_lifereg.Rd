% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surv_param_lifereg.r
\name{define_surv_lifereg}
\alias{define_surv_lifereg}
\title{Define Parametric Distribution from SAS PROC LIFEREG Parameters}
\usage{
define_surv_lifereg(distribution, intercept, scale = 1, shape = NULL)
}
\arguments{
\item{distribution}{one of \code{"exponential"}, \code{"weibull"},
\code{"lnormal"}, \code{"llogistic"}, or \code{"gamma"}.}

\item{intercept}{the Intercept from the LIFEREG Parameter
Estimates table.}

\item{scale}{the Scale parameter from the LIFEREG output.
Defaults to 1 (used for exponential).}

\item{shape}{the Shape parameter from the LIFEREG output.
Only used for \code{distribution = "gamma"}.}
}
\value{
a \code{surv_parametric} object.
}
\description{
Define a parametric survival distribution using parameter estimates
from SAS PROC LIFEREG output. Parameters are converted from the
AFT parameterization to flexsurv's native parameterization.
}
\details{
The following conversions are applied:\tabular{lll}{
   \strong{SAS DIST=} \tab \strong{flexsurv distribution} \tab \strong{Conversion} \cr
   EXPONENTIAL \tab exp \tab \code{rate = exp(-intercept)} \cr
   WEIBULL \tab weibull \tab \code{shape = 1/scale}, \code{scale = exp(intercept)} \cr
   LNORMAL \tab lnorm \tab \code{meanlog = intercept}, \code{sdlog = scale} \cr
   LLOGISTIC \tab llogis \tab \code{shape = 1/scale}, \code{scale = exp(intercept)} \cr
   GAMMA \tab gengamma \tab \code{mu = intercept}, \code{sigma = scale}, \code{Q = shape} \cr
}

\subsection{Survival function formulations}{

In terms of LIFEREG parameters:
\itemize{
\item \strong{Exponential}: \code{S(t) = exp(-exp(-intercept) * t)}
\item \strong{Weibull}: \code{S(t) = exp(-(t / exp(intercept))^(1/scale))}
\item \strong{Lognormal}: \code{S(t) = 1 - Phi((log(t) - intercept) / scale)} where
\code{Phi()} is the standard normal CDF
\item \strong{Log-logistic}: \code{S(t) = 1 / (1 + (t / exp(intercept))^(1/scale))}
\item \strong{Generalized gamma}: No simple closed form;
\code{mu = intercept}, \code{sigma = scale}, \code{Q = shape};
see Prentice (1974)
}
}
}
\examples{

define_surv_lifereg("exponential", intercept = 3)
define_surv_lifereg("weibull", intercept = 3, scale = 0.8)
define_surv_lifereg("gamma", intercept = 2.3, scale = 0.4, shape = -0.03)

}
\references{
Prentice, R. L. (1974). A log gamma model and its maximum
likelihood estimation. Biometrika 61(3):539-544.
}
